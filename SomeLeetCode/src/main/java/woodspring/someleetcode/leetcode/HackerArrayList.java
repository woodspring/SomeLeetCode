package woodspring.someleetcode.leetcode;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HackerArrayList {
	private static final Logger logger = LoggerFactory.getLogger(HackerArrayList.class);
	
	/**
	 * @param inputList
	 * @return
	 */
	/**
	 * @param inputList
	 * @return
	 */
	/**
	 * @param inputList
	 * @return
	 */
	/**
	 * @param inputList
	 * @return
	 */
	public List<String> checkList( List<String> inputList) {
		List<String> retSult = new ArrayList<String>();
		
//		if ( inputList == null || inputList.isEmpty() || inputList.size() < 2) {
//			retSult.add("ERROR !!");
//			return retSult;
//		}
//		int lenX = Integer.valueOf(inputList.get(0));
//		ArrayList< ArrayList<Integer>> context = new ArrayList< ArrayList<Integer>>();
//		for (int ind = 1; ind <= lenX; ind++) {
//			String aString = inputList.get( ind);
//			String[] strList = aString.split(" ");
//			ArrayList<Integer> intArr = new ArrayList<>();
//			for ( int ind1 = 0; ind1 < strList.length; ind1++) {
//				intArr.add( Integer.valueOf( strList[ind1]));				
//			}
//			ArrayList<Integer> intList = Arrays.asList(strList).stream().map( org.apache.commons.lang3.math.NumberUtils::toInt) //Arrays.asList( aString.split(" "))
//										//.stream()
//										//.map(Integer::parseInt)
//										.collect(Collectors.toList());
//			context.add( intList);		
//		}
//		int indQ = lenX +1;
//		if ( indQ >= inputList.size()) return retSult; // no Query
//		int numQ = 
//		
//		
//		
		return retSult;
		
	}
			

}
